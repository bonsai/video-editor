{
  "name": "video-editor",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AI-native / OSS video-editor monorepo",
  "workspaces": [
    "packages/*",
    "apps/*",
    "engines/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "lint": "echo 'lint not configured yet'",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
